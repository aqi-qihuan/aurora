# Aurora博客系统部署指南

## 目录

- [1. 环境准备](#1-环境准备)
- [2. 中间件部署](#2-中间件部署)
- [3. 配置初始化](#3-配置初始化)
- [4. 后端部署](#4-后端部署)
- [5. 前端部署](#5-前端部署)
- [6. Nginx配置](#6-nginx配置)
- [7. 性能与内存优化](#7-性能与内存优化)
- [8. 常见问题](#8-常见问题)

---

## 1. 环境准备

### 1.1 服务器要求

#### 推荐配置

| 配置 | ES搜索策略 | MySQL搜索策略 |
|------|-----------|---------------|
| CPU | 2核 | 2核 |
| 内存 | 4GB | 2GB |
| 带宽 | 1M+OSS 或 3M+MinIO | 1M+OSS 或 3M+MinIO |

#### 内存优化效果

通过极限优化配置（详见[内存优化总结](./内存优化总结.md)），在2核4G服务器上的实际内存占用：

| 组件 | 内存占用 | 占比 | 优化配置 |
|------|---------|------|---------|
| SpringBoot | 342.8MiB | 22.3% | JDK 25, -Xms64m -Xmx128m + SerialGC |
| Elasticsearch | 652.9MiB | 42.5% | -Xms64m -Xmx160m + G1GC + IK分词器 |
| MySQL | 148.3MiB | 9.6% | 连接池30、缓冲池96M |
| Maxwell | 150.4MiB | 9.8% | 自动优化 |
| MinIO | 130.1MiB | 8.5% | 缓存优化 |
| RabbitMQ | 107.8MiB | 7.0% | 默认配置 |
| Redis | 4.0MiB | 0.3% | 24MB内存限制 |
| Nginx | 3.1MiB | 0.2% | 默认配置 |
| **总计** | **1539.44MiB** | **100%** | **内存占用率约72.7%** |

> **注意**：如果不需要MinIO，可节省约130MB内存。

### 1.2 Docker安装

#### Ubuntu系统

```shell
# 更新软件包索引
apt-get update

# 安装依赖包
apt-get install -y ca-certificates curl gnupg lsb-release

# 添加Docker官方GPG密钥
curl -fsSL https://mirrors.aliyun.com/docker-ce/linux/ubuntu/gpg | gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# 设置稳定版仓库
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://mirrors.aliyun.com/docker-ce/linux/ubuntu $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null

# 安装Docker Engine
apt-get update
apt-get install -y docker-ce docker-ce-cli containerd.io

# 启动Docker并设置开机自启
systemctl start docker
docker version
systemctl enable docker
```

---

## 2. 中间件部署

使用docker-compose统一部署所有中间件，方便管理和维护。

### 2.1 创建项目目录

```shell
mkdir -p /opt/aurora
cd /opt/aurora
```

### 2.2 创建docker-compose.yml

在`/opt/aurora/`目录下创建`docker-compose.yml`：

```yaml
version: "3.8"
services:
  mysql:
    image: mysql:8.0.32
    container_name: aurora-mysql
    environment:
      MYSQL_ROOT_PASSWORD: 你的密码
      MYSQL_DATABASE: aurora
      TZ: Asia/Shanghai
    ports:
      - "3306:3306"
    volumes:
      - /opt/aurora/mysql/data:/var/lib/mysql
    networks:
      - aurora-network
    restart: always
    command: >
      --innodb_buffer_pool_size=96M
      --innodb_log_buffer_size=4M
      --innodb_flush_log_at_trx_commit=2
      --sync_binlog=100
      --max_connections=30
      --table_open_cache=100
      --thread_cache_size=5
      --performance_schema=OFF
      --skip-name-resolve
      --tmp_table_size=4M
      --max_heap_table_size=4M
      --innodb_flush_method=O_DSYNC

  redis:
    image: redis:7.0.8
    container_name: aurora-redis
    ports:
      - "6379:6379"
    volumes:
      - /opt/aurora/redis/data:/data
    networks:
      - aurora-network
    restart: always
    command: >
      redis-server --requirepass 你的密码
        --maxmemory 24mb
        --maxmemory-policy allkeys-lru
        --save ""
        --appendonly no
        --maxclients 100
        --tcp-backlog 128
        --timeout 300

  rabbitmq:
    image: rabbitmq:3.11.9-management
    container_name: aurora-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: myroot
      RABBITMQ_DEFAULT_PASS: 你的密码
      TZ: Asia/Shanghai
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - /opt/aurora/rabbitmq/data:/var/lib/rabbitmq
    networks:
      - aurora-network
    restart: always

  elasticsearch:
    image: elasticsearch:8.13.4
    container_name: aurora-elasticsearch
    environment:
      discovery.type: single-node
      ES_JAVA_OPTS: "-Xms64m -Xmx160m -XX:+UseG1GC -XX:MaxGCPauseMillis=200"
      TZ: Asia/Shanghai
      bootstrap.memory_lock: "true"
      xpack.security.enabled: "false"
      indices.query.bool.max_clause_count: "1024"
      indices.queries.cache.size: "5%"
      indices.fielddata.cache.size: "0%"
      indices.memory.index_buffer_size: "10mb"
      thread_pool.write.queue_size: "100"
      indices.breaker.total.limit: "85%"
      indices.breaker.fielddata.limit: "10%"
      thread_pool.search.size: "1"
      thread_pool.search.throttled.size: "1"
      thread_pool.index.size: "1"
      thread_pool.write.size: "1"
    ports:
      - "9200:9200"
      - "9300:9300"
    volumes:
      - /opt/aurora/elasticsearch/data:/usr/share/elasticsearch/data
    networks:
      - aurora-network
    restart: always
    ulimits:
      memlock:
        soft: -1
        hard: -1

  minio:
    image: bitnami/minio:2023.12.7
    container_name: aurora-minio
    hostname: minio-server
    environment:
      MINIO_ROOT_USER: "你的用户名."
      MINIO_ROOT_PASSWORD: "你的密码."
      MINIO_DEFAULT_BUCKETS: "aurora"
      MINIO_SERVER_URL: "http://127.0.0.1:9000"
      TZ: Asia/Shanghai
      MINIO_CACHE_DRIVES: /tmp/cache
      MINIO_CACHE_EXPIRY: 90
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - /opt/aurora/minio/data:/bitnami/minio/data
    networks:
      - aurora-network
    restart: always

  nginx:
    image: nginx:1.23.3
    container_name: aurora-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /opt/aurora/nginx/conf:/etc/nginx
      - /opt/aurora/nginx/html:/usr/share/nginx/html
      - /opt/aurora/vue/blog:/usr/share/nginx/blog
      - /opt/aurora/vue/admin:/usr/share/nginx/admin
      - /opt/aurora/nginx/cert:/etc/nginx/cert
    networks:
      - aurora-network
    restart: always

  springboot:
    image: eclipse-temurin:25-jre-alpine
    container_name: aurora-springboot
    environment:
      SPRING_PROFILES_ACTIVE: prod
      TZ: Asia/Shanghai
    ports:
      - "8080:8080"
    volumes:
      - /opt/aurora/app/aurora-springboot-0.0.1.jar:/app/blog.jar
    networks:
      - aurora-network
    restart: always
    command: >
      java -Xms64m -Xmx128m -XX:+UseSerialGC
          -XX:MetaspaceSize=64m -XX:MaxMetaspaceSize=128m
          -XX:ReservedCodeCacheSize=32m -Xss256k
          -XX:+UseCompressedOops
          -XX:+UseCompressedClassPointers
          -XX:+UseStringDeduplication
          -XX:+OptimizeStringConcat
          -XX:+AlwaysPreTouch
          -Djava.security.egd=file:/dev/./urandom
          -Dspring.jmx.enabled=false
          -Dspring.backgroundpreinitializer.ignore=true
          -jar /app/blog.jar

  maxwell:
    image: zendesk/maxwell:latest
    container_name: aurora-maxwell
    volumes:
      - /opt/aurora/maxwell/config/config.properties:/app/config.properties
    networks:
      - aurora-network
    restart: always
    command: >
      bin/maxwell --host=aurora-mysql --user=root --password=你的密码
        --producer=rabbitmq --rabbitmq_host=aurora-rabbitmq
        --rabbitmq_user=myroot --rabbitmq_pass=你的密码
        --rabbitmq_exchange=maxwell --rabbitmq_exchange_type=topic
        --rabbitmq_routing_key_template=%{database}.%{table}
        --client_id=maxwell_prod

networks:
  aurora-network:
    external: true
```

**重要提示**：请将配置中的`你的密码`、`你的用户名`替换为实际密码。

### 2.3 启动所有中间件

```shell
cd /opt/aurora
docker-compose up -d
```

### 2.4 查看服务状态

```shell
docker ps
docker stats --no-stream --format 'table {{.Name}}\t{{.MemUsage}}\t{{.MemPerc}}'
```

---

## 3. 配置初始化

### 3.1 安装IK分词器

进入Elasticsearch容器：

```shell
docker exec -it aurora-elasticsearch /bin/bash
```

安装IK分词器：

```shell
./bin/elasticsearch-plugin install https://release.infinilabs.com/analysis-ik/stable/elasticsearch-analysis-ik-8.13.4.zip
```

> **注意**：IK分词器版本必须与Elasticsearch版本一致（8.13.4）

退出容器并重启：

```shell
exit
docker restart aurora-elasticsearch
```

验证安装：

```shell
docker exec aurora-elasticsearch elasticsearch-plugin list
```

测试分词功能：

```shell
curl -X POST 'http://服务器IP:9200/_analyze' \
  -H 'Content-Type: application/json' \
  -d '{
    "tokenizer": "ik_smart",
    "text": "我爱技术"
  }'
```

### 3.2 创建外部网络

如果网络不存在，需先创建：

```shell
docker network create aurora_aurora-network
```

---

## 4. 后端部署

### 4.1 修改配置文件

修改`aurora-springboot/src/main/resources/application-prod.yml`：

```yaml
spring:
  datasource:
    url: jdbc:mysql://aurora-mysql:3306/aurora?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: 你的密码

  redis:
    host: aurora-redis
    port: 6379
    password: 你的密码

  rabbitmq:
    host: aurora-rabbitmq
    port: 5672
    username: myroot
    password: 你的密码

elasticsearch:
  host: aurora-elasticsearch
  port: 9200
  # 已禁用安全认证，无需用户名密码
```

### 4.2 第三方服务配置

**阿里云OSS**：
- 在阿里云控制台创建OSS bucket
- 获取AccessKey和SecretKey
- 在配置文件中填写相关参数

**QQ互联**：
- 在QQ互联平台申请应用
- 获取APP_ID和APP_KEY
- 配置回调地址

### 4.3 打包应用

```shell
cd aurora-springboot
mvn clean package -DskipTests
```

### 4.4 上传jar包

```shell
mkdir -p /opt/aurora/app
```

将`aurora-springboot-0.0.1.jar`上传到`/opt/aurora/app/`目录。

### 4.5 创建Dockerfile

在`/opt/aurora/app/`目录下创建`Dockerfile`：

```dockerfile
FROM eclipse-temurin:25-jre-alpine
VOLUME /tmp
ADD aurora-springboot-0.0.1.jar blog.jar

# JVM 极限优化参数
ENTRYPOINT ["java", "-Xms64m", "-Xmx128m", "-XX:+UseSerialGC",
  "-XX:MetaspaceSize=64m", "-XX:MaxMetaspaceSize=128m",
  "-XX:ReservedCodeCacheSize=32m", "-Xss256k",
  "-XX:+UseCompressedOops", "-XX:+UseCompressedClassPointers",
  "-XX:+UseStringDeduplication", "-XX:+OptimizeStringConcat",
  "-XX:+AlwaysPreTouch", "-Djava.security.egd=file:/dev/./urandom",
  "-Dspring.jmx.enabled=false", "-Dspring.backgroundpreinitializer.ignore=true",
  "-jar", "/blog.jar"]
```

### 4.6 构建并启动容器

```shell
cd /opt/aurora/app
docker build -t aurora-springboot:latest .
docker run -d --name aurora-springboot \
  --network aurora_aurora-network \
  -p 8080:8080 \
  --restart=always \
  aurora-springboot:latest
```

### 4.7 查看启动日志

```shell
docker logs -f aurora-springboot
```

### 4.8 访问测试

在浏览器访问：`http://服务器IP:8080`

---

## 5. 前端部署

### 5.1 修改配置文件

**前台配置** (`aurora-blog/src/config/config.ts`)：

```typescript
captcha: {
  TENCENT_CAPTCHA: '你自己的天御验证码'
},
qqLogin: {
  QQ_APP_ID: '你自己的APP_ID',
  QQ_REDIRECT_URI: 'https://你的前台域名/oauth/login/qq'
},
```

**前台页面** (`aurora-blog/public/index.html`)：

```html
<script
  src="http://connect.qq.com/qc_jssdk.js"
  data-appid="你自己的APP_ID"
  data-redirecturi="https://你的前台域名/oauth/login/qq"></script>
```

> **注意**：如不使用HTTPS，请注释掉 `<meta http-equiv="Content-Security-Policy" content="upgrade-insecure-requests" />`

### 5.2 安装依赖并打包

分别在`aurora-blog`和`aurora-admin`目录下执行：

```shell
npm install
npm run build
```

**推荐版本**：npm 8.3.1, vue-cli 5.0.6

**本地调试**：

```shell
npm run serve
```

### 5.3 部署到服务器

```shell
mkdir -p /opt/aurora/vue
```

将打包后的文件：
- `aurora-blog/dist` 重命名为 `blog`，上传到 `/opt/aurora/vue/blog/`
- `aurora-admin/dist` 重命名为 `admin`，上传到 `/opt/aurora/vue/admin/`

### 5.4 初始账号密码

```
用户名：admin@163.com
密码：123456
```

⚠️ **第一次登录后一定要修改密码！！！**

---

## 6. Nginx配置

### 6.1 HTTP方式

在`/opt/aurora/nginx/`目录下创建`nginx.conf`：

```nginx
events {
    worker_connections  1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;

    client_max_body_size     50m;
    client_body_buffer_size  10m;
    client_header_timeout    1m;
    client_body_timeout      1m;

    gzip on;
    gzip_min_length  1k;
    gzip_buffers     4 16k;
    gzip_comp_level  4;
    gzip_types text/plain application/javascript application/x-javascript text/css application/xml text/javascript application/x-httpd-php image/jpeg image/gif image/png;
    gzip_vary on;

    server {
        listen       80;
        server_name  前台域名;

        location / {
            root   /usr/share/nginx/blog;
            index  index.html index.htm;
            try_files $uri $uri/ /index.html;
        }

        location ^~ /api/ {
            proxy_pass http://aurora-springboot:8080/;
            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
        }
    }

    server {
        listen       80;
        server_name  后台域名;

        location / {
            root   /usr/share/nginx/admin;
            index  index.html index.htm;
            try_files $uri $uri/ /index.html;
        }

        location ^~ /api/ {
            proxy_pass http://aurora-springboot:8080/;
            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
        }
    }
}
```

### 6.2 HTTPS方式

```nginx
events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;

    client_max_body_size     50m;
    client_body_buffer_size  10m;
    client_header_timeout    1m;
    client_body_timeout      1m;

    gzip on;
    gzip_min_length 1k;
    gzip_buffers     4 16k;
    gzip_comp_level 4;
    gzip_types text/plain application/javascript application/x-javascript text/css application/xml text/javascript application/x-httpd-php image/jpeg image/gif image/png;
    gzip_vary on;

    # SSL配置
    ssl_session_timeout 5m;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4;
    ssl_protocols TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;

    # HTTP到HTTPS重定向
    server {
        listen       80;
        server_name  前台域名 后台域名 minio域名;
        return 301 https://$host$request_uri;
    }

    # 前台配置
    server {
        listen  443 ssl;
        server_name  前台域名;

        ssl_certificate /etc/nginx/cert/前台域名_bundle.pem;
        ssl_certificate_key /etc/nginx/cert/前台域名.key;

        location / {
            root   /usr/share/nginx/blog;
            index  index.html index.htm;
            try_files $uri $uri/ /index.html;
        }

        location /api/ {
            proxy_pass http://aurora-springboot:8080/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_connect_timeout 30s;
            proxy_send_timeout 30s;
            proxy_read_timeout 30s;
            proxy_intercept_errors on;
        }
    }

    # 后台配置
    server {
        listen  443 ssl;
        server_name  后台域名;

        ssl_certificate /etc/nginx/cert/后台域名_bundle.pem;
        ssl_certificate_key /etc/nginx/cert/后台域名.key;

        location / {
            root   /usr/share/nginx/admin;
            index  index.html index.htm;
            try_files $uri $uri/ /index.html;
        }

        location /api/ {
            proxy_pass http://aurora-springboot:8080/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_connect_timeout 30s;
            proxy_send_timeout 30s;
            proxy_read_timeout 30s;
            proxy_intercept_errors on;
        }
    }

    # MinIO配置
    server {
        listen  443 ssl;
        server_name  minio域名;

        ssl_certificate /etc/nginx/cert/minio域名_bundle.pem;
        ssl_certificate_key /etc/nginx/cert/minio域名.key;

        location / {
            proxy_pass http://aurora-minio:9000/aurora/;
            proxy_set_header Host $proxy_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_connect_timeout 30s;
            proxy_send_timeout 30s;
            proxy_read_timeout 30s;
            proxy_intercept_errors on;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }
}
```

### 6.3 重启Nginx

```shell
docker restart aurora-nginx
```

---

## 7. 性能与内存优化

### 7.1 性能测试结果

各组件响应时间（2026-03-09 测试）：

| 测试项 | 耗时 | 状态 |
|--------|------|------|
| SpringBoot 首页 | 22.2ms | ✅ 快速 |
| ES 直接搜索 | 10.4ms | ✅ 极快 |
| 文章列表 API | 15.6ms | ✅ 快速 |
| 文章搜索 API | 10.4ms | ✅ 极快 |
| 文章详情 API | 10.0ms | ✅ 极快 |
| 并发测试 (10次) | 5.2-7.8ms | ✅ 稳定 |
| Redis PING | 0.116s | ✅ 正常 |
| IK 分词测试 | 42ms | ✅ 正常 |

**结论**：极限优化配置下性能依然优秀，系统运行稳定。

### 7.2 快速验证内存优化

```bash
# 查看所有容器内存使用
docker stats --no-stream --format 'table {{.Name}}\t{{.MemUsage}}\t{{.MemPerc}}'

# 查看SpringBoot JVM参数
docker exec aurora-springboot jcmd 1 VM.flags

# 查看Elasticsearch JVM参数
docker exec aurora-elasticsearch env | grep ES_JAVA_OPTS

# 查看MySQL配置
docker exec aurora-mysql mysql -uroot -p你的密码 -e "SHOW VARIABLES LIKE 'innodb_buffer_pool_size';"
```

### 7.3 内存优化要点

1. **SpringBoot JVM极限优化**：64MB-128MB堆内存 + SerialGC (JDK 25)
2. **Elasticsearch优化**：64MB-160MB堆内存 + G1GC + IK分词器 + 断路器调优
3. **MySQL优化**：连接池30、缓冲池96M、表缓存100等极限调优
4. **Redis优化**：限制24MB内存，禁用持久化
5. **整体效果**：总内存占用约1.51GB，内存占用率约72.7%

> 详细优化文档请参考 [内存优化总结](./内存优化总结.md)

---

## 8. 常见问题

### 8.1 Docker相关

**容器一直重启**

```bash
docker logs 容器名称
```

**内存不足导致OOM**

- 检查docker-compose.yml中的内存限制配置
- 参考[内存优化总结](./内存优化总结.md)进行调整

**网络连接问题**

```bash
docker network ls
docker network inspect aurora_aurora-network
```

### 8.2 应用相关

**SpringBoot启动失败**

- 检查JVM参数是否正确
- 确认数据库、Redis等连接配置
- 查看日志：`docker logs aurora-springboot`

**Elasticsearch无法启动**

- 确认JVM内存设置（不低于64MB）
- 检查数据目录权限
- 查看日志：`docker logs aurora-elasticsearch`

**数据库连接失败**

- 确认数据库密码正确
- 检查MySQL连接数限制
- 验证网络连通性

### 8.3 其他问题

**如何修改网站基本信息**

打开后台管理系统，找到【系统管理 → 网站管理】，在此修改。

**ES安全认证**

当前配置已禁用ES安全认证（`xpack.security.enabled: "false"`），如需启用请修改docker-compose.yml。

---

## 更新日志

| 日期 | 更新内容 |
|------|---------|
| 2026-03-09 | 更新内存优化配置（第三轮优化），ES断路器调优解决429错误 |
| 2026-03-09 | 更新docker-compose.yml所有中间件配置，与实际部署一致 |
| 2026-03-09 | 更新SpringBoot使用JDK 25 + 64MB-128MB极限内存配置 |
| 2026-03-09 | 新增性能测试结果，验证极限优化配置下的性能表现 |
| 2026-03-09 | 禁用Elasticsearch安全认证，简化部署流程 |
| 2026-03-07 | 更新Elasticsearch版本至8.13.4 |
| 2026-03-07 | 新增docker-compose部署方式，统一管理中间件 |
| 2026-03-07 | 新增Ubuntu系统Docker安装说明 |
| 2026-03-07 | 服务器要求更新，包含实际内存占用数据 |

---

**文档最后更新**：2026-03-09
**服务器配置**：4核4G / 2核2G
**推荐部署方式**：Docker Compose
**总内存占用**：1539.44MiB (4核4G环境)
