# 首页作品集（Portfolio）方案

> 目标：在博客首页 Feature 轮播与文章列表之间，新增「作品集」区块，展示 GitHub 开源项目，通过定时任务自动同步仓库数据。

---

## 一、方案概览

| 维度 | 选型 | 说明 |
|:-----|:-----|:-----|
| 内容来源 | GitHub REST API | `/users/{username}/repos`，按更新时间倒序 |
| 同步方式 | 定时任务 | 复用现有 scheduler（Go: robfig/cron / Java: Quartz） |
| 数据落地 | 新建 `t_portfolio` 表 | 缓存仓库快照，避免每次请求都打 GitHub |
| 配置存储 | 扩展 `t_website_config` | 存 GitHub username / token / 筛选规则 |
| 卡片形态 | 横向卡片 | 与文章卡片区分，突出 star/语言/topics |
| 前端组件 | 新增 `Portfolio` 组件族 | 完全复用现有 Feature/Title/Card 模式 |

**核心原则**：不引入新范式，顺着现有 `model → service → handler` 分层和 `Feature/ArticleCard` 组件化模式走。

---

## 二、数据模型

### 2.1 新建表 `t_portfolio`

```sql
CREATE TABLE `t_portfolio` (
  `id`                int          NOT NULL AUTO_INCREMENT COMMENT '主键',
  `repo_id`           bigint       NOT NULL COMMENT 'GitHub 仓库 ID（唯一）',
  `name`              varchar(128) NOT NULL COMMENT '仓库名',
  `full_name`         varchar(255) DEFAULT NULL COMMENT 'owner/repo',
  `description`       varchar(500) DEFAULT NULL COMMENT '仓库描述',
  `html_url`          varchar(500) NOT NULL COMMENT '仓库地址',
  `homepage`          varchar(500) DEFAULT NULL COMMENT '演示地址（homepage）',
  `language`          varchar(64)  DEFAULT NULL COMMENT '主语言',
  `stargazers_count`  int          DEFAULT 0  COMMENT 'star 数',
  `forks_count`       int          DEFAULT 0  COMMENT 'fork 数',
  `topics`            varchar(500) DEFAULT NULL COMMENT '话题标签，JSON 数组',
  `repo_created_at`   datetime     DEFAULT NULL COMMENT '仓库创建时间',
  `repo_updated_at`   datetime     DEFAULT NULL COMMENT '仓库更新时间',
  `cover`             varchar(500) DEFAULT NULL COMMENT '自定义封面（后台覆盖）',
  `sort`              int          DEFAULT 0  COMMENT '排序权重，越大越靠前',
  `is_featured`       tinyint(1)   DEFAULT 0  COMMENT '是否首页置顶展示',
  `is_visible`        tinyint(1)   DEFAULT 1  COMMENT '是否展示（0=隐藏）',
  `create_time`       datetime     DEFAULT CURRENT_TIMESTAMP,
  `update_time`       datetime     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_repo_id` (`repo_id`),
  KEY `idx_visible_featured` (`is_visible`, `is_featured`, `sort`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='GitHub 作品集';
```

### 2.2 封面策略

GitHub 仓库无官方截图，采用分级方案：

1. **默认**：前端用「语言主题色块 + 仓库名首字母」生成占位封面（纯 CSS，零存储）
2. **可选**：后台手动上传封面 URL 写入 `cover` 字段，覆盖默认
3. **进阶**（可选）：后端抓取 `homepage` 的 `og:image`，异步缓存——成本较高，建议二期

---

## 三、后端实现

> Go 与 Java 双后端 API 兼容，以下按 Go 版（主力轻量版）详述，Java 版结构对应。

### 3.1 Model — `internal/model/portfolio.go`

```go
package model

import "time"

type Portfolio struct {
    ID              uint      `gorm:"primarykey" json:"id"`
    RepoID          int64     `gorm:"uniqueIndex" json:"repoId"`
    Name            string    `json:"name"`
    FullName        string    `json:"fullName"`
    Description     string    `json:"description"`
    HtmlURL         string    `json:"htmlUrl"`
    Homepage        string    `json:"homepage"`
    Language        string    `json:"language"`
    StargazersCount int       `json:"stargazersCount"`
    ForksCount      int       `json:"forksCount"`
    Topics          string    `gorm:"type:text" json:"topics"` // JSON 数组字符串
    RepoCreatedAt   *time.Time `json:"repoCreatedAt"`
    RepoUpdatedAt   *time.Time `json:"repoUpdatedAt"`
    Cover           string    `json:"cover"`
    Sort            int       `json:"sort"`
    IsFeatured      int8      `json:"isFeatured"`
    IsVisible       int8      `json:"isVisible"`
    CreateTime      time.Time `json:"createTime"`
    UpdateTime      time.Time `json:"updateTime"`
}

func (Portfolio) TableName() string { return "t_portfolio" }
```

### 3.2 Service — `internal/service/portfolio_service.go`

核心两类方法：**对外查询** + **GitHub 同步**。

```go
type PortfolioService struct {
    db  *gorm.DB
    rdb *redis.Client
}

// 对外：首页展示（置顶优先 + 排序）
func (s *PortfolioService) ListFeatured(ctx context.Context) ([]model.Portfolio, error) {
    var list []model.Portfolio
    err := s.db.Where("is_visible = 1").
        Order("is_featured DESC, sort DESC, stargazers_count DESC").
        Limit(6).Find(&list).Error
    return list, err
}

// 对外：全部作品（分页）
func (s *PortfolioService) ListAll(ctx context.Context, current, size int) (...) { ... }

// GitHub 同步核心逻辑
func (s *PortfolioService) SyncFromGitHub(ctx context.Context) error {
    cfg := s.getWebsiteConfig() // 从 t_website_config 读 username/token
    repos, err := s.fetchGitHubRepos(ctx, cfg.GitHubUsername, cfg.GitHubToken)
    if err != nil {
        return err
    }
    // upsert：按 repo_id 唯一键，存在则更新统计，保留 cover/sort/is_featured/is_visible
    for _, repo := range repos {
        s.upsertPortfolio(repo)
    }
    // 清理 Redis 缓存，让前台拉新数据
    s.rdb.Del(ctx, "portfolio:featured")
    return nil
}
```

**GitHub API 调用要点**：
- 端点：`GET https://api.github.com/users/{username}/repos?sort=updated&per_page=100`
- 带 Token（`Authorization: Bearer xxx`）提升限额到 5000/h，无 Token 仅 60/h
- User-Agent 头必填，否则 403
- `topics` 字段需额外请求或用 `Accept: application/vnd.github.mercy-preview+json`

### 3.3 定时任务 — `internal/scheduler/github_sync_job.go`

完全仿照 `NewUniqueViewJob` 的写法：

```go
type GitHubSyncJob struct {
    db  *gorm.DB
    rdb *redis.Client
}

func NewGitHubSyncJob(db *gorm.DB, rdb *redis.Client) *GitHubSyncJob {
    return &GitHubSyncJob{db: db, rdb: rdb}
}

func (j *GitHubSyncJob) Run(ctx context.Context, params ...interface{}) error {
    svc := service.NewPortfolioService(j.db, j.rdb)
    return svc.SyncFromGitHub(ctx)
}
```

**注册**（`job_invoke.go` 的 `InitTaskRegistry`）：

```go
TaskRegistry[prefix+".syncGitHubRepos"] = NewGitHubSyncJob(scheduler.db, scheduler.rdb).Run
```

**调度记录**（`aurora.sql` 的 `t_job` 表插入）：

```sql
INSERT INTO `t_job` (`job_name`, `invoke_target`, `cron_expression`, `status`) 
VALUES ('GitHub作品集同步', 'auroraQuartz.syncGitHubRepos', '0 0 3 * * ?', 1);
-- 每天 03:00 执行一次
```

### 3.4 接口 — `internal/handler/portfolio_handler.go`

| 方法 | 路径 | 权限 | 说明 |
|:-----|:-----|:-----|:-----|
| GET | `/api/portfolios/featured` | 公开 | 首页置顶作品（6 条） |
| GET | `/api/portfolios` | 公开 | 全部作品分页 |
| GET | `/api/admin/portfolios` | 管理员 | 后台列表（含隐藏项） |
| PUT | `/api/admin/portfolios/{id}` | 管理员 | 编辑封面/排序/置顶/可见性 |
| POST | `/api/admin/portfolios/sync` | 管理员 | 手动触发同步 |

### 3.5 配置扩展 — `t_website_config`

在现有 `config` JSON 中追加：

```json
{
  "githubUsername": "zhouyqxy",
  "githubToken": "",
  "portfolioEnabled": true,
  "portfolioSort": "updated"
}
```

> Token 为敏感信息，建议：存环境变量优先，DB 仅存占位；后台展示时脱敏。

---

## 四、前端实现（aurora-blog）

### 4.1 新增组件 — `src/components/Portfolio/`

完全对标 `Feature` 组件族结构：

```
src/components/Portfolio/
├── index.ts                    # 导出
└── src/
    ├── Portfolio.vue           # 容器（对标 Feature.vue，含 slot + Title）
    ├── PortfolioList.vue       # 卡片网格（对标 FeatureList.vue）
    └── PortfolioCard.vue       # 单卡片（对标 ArticleCard.vue）
```

**Portfolio.vue**（容器，复用 Title 组件）：

```vue
<template>
  <div id="portfolio">
    <Title :title="'titles.portfolio'" icon="project" />
    <slot />
  </div>
</template>
```

**PortfolioCard.vue**（横向卡片核心）：

```vue
<template>
  <div class="portfolio-card">
    <div class="portfolio-cover">
      <img v-if="item.cover" v-lazy="item.cover" alt="" />
      <div v-else class="cover-fallback" :style="langStyle">
        <span>{{ item.name?.charAt(0)?.toUpperCase() }}</span>
      </div>
    </div>
    <div class="portfolio-content">
      <div class="portfolio-header">
        <svg-icon icon-class="github" />
        <a :href="item.htmlUrl" target="_blank" class="portfolio-name">{{ item.name }}</a>
        <span class="star-badge">★ {{ item.stargazersCount }}</span>
      </div>
      <p class="portfolio-desc">{{ item.description }}</p>
      <div class="portfolio-meta">
        <span class="lang-dot" :style="{ background: langColor }"></span>
        {{ item.language }}
        <span class="divider">·</span>
        {{ relativeTime(item.repoUpdatedAt) }}
      </div>
      <ul class="portfolio-topics" v-if="topics.length">
        <li v-for="t in topics.slice(0,4)" :key="t"># {{ t }}</li>
      </ul>
      <div class="portfolio-actions">
        <a :href="item.htmlUrl" target="_blank" class="btn-primary">查看仓库</a>
        <a v-if="item.homepage" :href="item.homepage" target="_blank" class="btn-ghost">在线演示</a>
      </div>
    </div>
  </div>
</template>
```

### 4.2 改造首页 — `src/views/Home.vue`

在 Feature 与文章列表之间插入：

```vue
<Feature v-if="themeConfig.feature">
  <FeatureList />
</Feature>

<!-- 新增作品集区块 -->
<Portfolio v-if="themeConfig.portfolioEnabled && portfolios.length">
  <PortfolioList :list="portfolios" />
</Portfolio>

<span v-if="themeConfig.feature">
  <Title id="article-list" :title="'titles.articles'" icon="article" />
</span>
```

`setup()` 中新增数据拉取：

```js
const portfolios = ref([])
const fetchPortfolios = () => {
  api.getPortfoliosFeatured().then(({ data }) => {
    if (data.flag) portfolios.value = data.data
  })
}
onMounted(() => {
  fetchTopAndFeatured()
  fetchPortfolios()   // 新增
  fetchCategories()
  fetchArticles()
})
```

### 4.3 API — `src/api/api.ts`

```js
getPortfoliosFeatured: () => axios.get('/api/portfolios/featured'),
getPortfolios: (params) => axios.get('/api/portfolios', { params }),
```

### 4.4 语言主题色映射

前端维护一份语言→颜色映射表（`langColor.ts`），用于卡片色点和占位封面：

```js
export const LANG_COLORS = {
  JavaScript: '#f1e05a', TypeScript: '#3178c6', Go: '#00ADD8',
  Java: '#b07219', Vue: '#41b883', Python: '#3572A5',
  // ... 完整列表见 github-linguist
}
```

---

## 五、后台管理（aurora-admin-v3）

新增「作品集管理」菜单，复用相册管理页的交互模式：

| 功能 | 说明 |
|:-----|:-----|
| 列表 | 展示所有作品（含隐藏项），显示 star/语言/可见性 |
| 手动同步 | 按钮 → `POST /api/admin/portfolios/sync` |
| 编辑 | 弹窗：自定义封面、排序权重、置顶、显示/隐藏 |
| 删除 | 软删除（设 `is_visible=0` 或硬删除） |

菜单注册：`t_menu` 表插入菜单项 + `t_role_menu` 关联权限。

---

## 六、实施步骤（建议顺序）

1. **建表**：执行 `t_portfolio` DDL + `t_job` 插入同步任务记录
2. **后端**（Go 先行）：
   - `model/portfolio.go` → `service/portfolio_service.go` → `scheduler/github_sync_job.go` → `handler/portfolio_handler.go`
   - 注册路由 + 注册 `TaskRegistry`
   - 配置 GitHub username/token
3. **前端**（aurora-blog）：
   - 新增 `Portfolio` 组件族
   - 改造 `Home.vue` + `api.ts`
   - 补充 i18n（`titles.portfolio` 等）
4. **后台**（aurora-admin-v3）：
   - 新增作品集管理页
   - 菜单权限注册
5. **联调验证**：手动触发同步 → 首页查看效果 → 调整排序/置顶

---

## 七、注意事项

| 风险点 | 应对 |
|:-------|:-----|
| GitHub API 限额 | 无 Token 60/h，带 Token 5000/h；同步任务每天一次足够 |
| Token 泄露 | 优先环境变量；后台脱敏展示；不入 git |
| 仓库过多 | `per_page=100` + 后台 `is_visible` 手动筛选 |
| 同步失败 | 复用 `t_job_log` 记录；保留上次成功快照不覆盖 |
| 首屏性能 | `featured` 仅 6 条；Redis 缓存 `portfolio:featured` |
| 双后端兼容 | Java 版同步实现 Quartz Job，接口路径保持一致 |
| fork 仓库过滤 | 同步时按 `fork == false` 过滤，只保留原创仓库（可配置） |

---

## 八、后续可扩展

- [ ] 抓取 `homepage` 的 og:image 自动生成封面
- [ ] 支持指定仓库白名单（`githubReposWhitelist` 配置）
- [ ] 卡片 hover 动效（复用现有 `stagger-fade` 动画）
- [ ] 独立作品集列表页 `/portfolios`（独立路由，首屏只放精选）
- [ ] GitHub Contributions 热力图集成到「关于」页
