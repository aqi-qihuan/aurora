# Aurora 博客系统内存优化总结

<p align="center">
  <img src="https://img.shields.io/badge/优化日期-2026--03--06~04--27-blue?style=for-the-badge" />
  <img src="https://img.shields.io/badge/服务器-广州4核4G-green?style=for-the-badge" />
  <img src="https://img.shields.io/badge/后端-SpringBoot_4.x_+_Go_1.26-blueviolet?style=for-the-badge" />
  <img src="https://img.shields.io/badge/总节省-~500MiB-orange?style=for-the-badge" />
</p>

---

## 📋 目录

- [服务器配置](#服务器配置)
- [双后端架构对比](#双后端架构对比)
- [优化效果对比](#优化效果对比)
- [详细优化配置](#详细优化配置)
- [Go 后端部署](#go-后端部署)
- [部署文件更新](#部署文件更新)
- [重启服务说明](#重启服务说明)
- [验证优化效果](#验证优化效果)
- [注意事项](#注意事项)
- [优化总结](#优化总结)
- [验证命令](#验证命令)
- [第四轮优化：GraalVM JIT](#第四轮优化graalvm-jit-模式迁移-2026-03-27)
- [第五轮优化：Spring Boot 4.x + Go 双后端](#第五轮优化spring-boot-4x--go-双后端-2026-04-27)

---

## 🖥️ 服务器配置

| 项目 | 详情 |
|:---|:---|
| 🌍 地域 | 广州 (ap-guangzhou) |
| 💻 实例 | 腾讯云轻量应用服务器 4核4G |
| ☕ Java 后端 | Spring Boot 4.1.0-M4 + JDK 25 |
| 🐹 Go 后端 | Go 1.26 + Gin 1.10 |
| 🔍 ES 版本 | 8.19.14（SpringBoot）/ 8.19.14（Go） |

---

## 🔄 双后端架构对比

Aurora 现提供 **Java** 和 **Go** 两种后端，共享中间件和前端，可按需切换：

| 对比项 | ☕ Spring Boot 4.x | 🐹 Go 1.26 |
|:-------|:-------------------|:-----------|
| **运行时内存** | ~280 MiB (JVM) | **~29 MiB** |
| **Docker 镜像** | ~180 MB (JRE) / ~500 MB (JDK) | **~5 MB** (Alpine) |
| **启动时间** | ~8s | **~0.3s** |
| **JVM 参数** | 需要 15+ 行调优 | 无需 JVM |
| **AI Agent** | — | tRPC-Agent-Go v1.8 (可选) |
| **适用场景** | 功能优先、生态丰富 | 资源受限、极致性能 |
| **API 兼容** | ✅ | ✅ 完全兼容 |

> 💡 切换后端只需修改 Nginx 反向代理指向，前端无需任何改动。

---

## 📊 优化效果对比

### 整体内存使用

| 指标 | 初始状态 | 第一轮 | 第二轮 | 第三轮 | 第四轮(GraalVM) | 总改善 |
|:------|:----------:|:-------:|:-------:|:-------:|:---------------:|:------:|
| 容器总内存(Java) | 约 1.90GiB | 约 1.45GiB | 约 1.44GiB | 约 1.51GiB | ~1.47GiB | ⬇️ **-430MiB (22.6%)** |
| 容器总内存(Go) | — | — | — | — | — | **~1.30GiB (1,336MiB)** |
| 可用内存(Java) | 933MiB | 1.3GiB | 1.31GiB | 910MiB | ~960MiB | ➡️ +27MiB |
| 可用内存(Go) | — | — | — | — | — | **~1.18GiB (1,216MiB)** |

### 各组件优化详情

| 组件 | 初始内存 | 第一轮 | 第二轮 | 第三轮 | 当前(SpringBoot) | Go 模式 | 节省 | 优化配置 |
|:------|:----------:|:-------:|:-------:|:-------:|:----------------:|:-------:|:------:|:--------|
| **后端服务** | 513.5MiB | 356.4MiB | 346.2MiB | 342.8MiB | 275.9MiB (GraalVM) | **~29MiB** | ⬇️ **-473MiB** | Go: 无JVM; Java: 极限JVM参数 |
| **MySQL** | 456.4MiB | 160.5MiB | 170.8MiB | 148.3MiB | 148.3MiB | 148.3MiB | ⬇️ **-308MiB** | 连接池、缓冲区深度优化 |
| **Elasticsearch** | 389.9MiB | 394.8MiB | 442.9MiB | 652.9MiB | ~390MiB | ~390MiB | ➡️ 持平 | `-Xms128m -Xmx160m` + 深度优化 |
| **Redis** | 5.6MiB | 8.0MiB | 6.1MiB | 4.0MiB | 4.0MiB | 4.0MiB | ⬇️ -1.6MiB | `maxmemory 48mb` |
| **RabbitMQ** | 109.7MiB | 109.6MiB | 112.8MiB | 107.8MiB | 107.8MiB | 107.8MiB | ⬇️ -1.9MiB | 未优化 |
| **MinIO** | 210.6MiB | 210.7MiB | 217.7MiB | 130.1MiB | 130.1MiB | 130.1MiB | ⬇️ -80MiB | 缓存优化 |
| **Maxwell** | 162.6MiB | 155MiB | 167.6MiB | 150.4MiB | 150.4MiB | 150.4MiB | ⬇️ -12MiB | 自动优化 |
| **Nginx** | 4.5MiB | 4.5MiB | 4.5MiB | 3.1MiB | 3.1MiB | 3.1MiB | ⬇️ -1.4MiB | 已优化 |

### Go 模式总内存对比

```
Java 后端模式:  ~1587 MiB (后端280 + 中间件1307)
Go 后端模式:    ~1336 MiB (后端29 + 中间件1307)
差距:           ~251 MiB (Go 节省 15.8%)

各组件实测内存（Go 模式）:
- aurora-go:              28.83 MiB
- aurora-mysql:           263.9 MiB
- aurora-redis:           5.148 MiB
- aurora-rabbitmq:        109.7 MiB
- aurora-elasticsearch:    578.8 MiB
- aurora-minio:           158 MiB
- aurora-maxwell:         180.3 MiB
- aurora-nginx:           11.47 MiB
总计:                     1336.148 MiB ≈ 1336 MiB
```

---

## ⚙️ 详细优化配置

### 1️⃣ SpringBoot JVM 极限优化

**docker-compose.yml 配置** (`/opt/aurora/docker-compose.yml`):

```yaml
command: >
  java -Xms48m -Xmx96m -XX:+UseSerialGC
      -XX:MetaspaceSize=128m -XX:MaxMetaspaceSize=192m
      -XX:ReservedCodeCacheSize=24m -Xss256k
      -XX:+UseCompressedOops
      -XX:+UseCompressedClassPointers
      -Djava.security.egd=file:/dev/./urandom
      -Dspring.jmx.enabled=false
      -Dspring.backgroundpreinitializer.ignore=true
      -Djdk.attach.allowAttachSelf=false
      -jar /app/blog.jar
```

**优化说明**：

| 参数 | 说明 | 效果 |
|:-----|:-----|:-----|
| `-XX:+UseSerialGC` | 使用 SerialGC 替代 G1GC | 小堆内存下更高效 |
| `-Xms48m -Xmx96m` | 堆内存极限压缩 | 大幅节省内存 |
| `-XX:MetaspaceSize=128m` | 元空间（Spring Boot 4.x 需要） | 适配新版框架 |
| `-XX:MaxMetaspaceSize=192m` | 元空间上限 | 防止无限膨胀 |
| `-XX:ReservedCodeCacheSize=24m` | 代码缓存压缩 | 降低内存消耗 |
| `-Xss256k` | 线程栈压缩 | 减少线程开销 |
| `-XX:+UseCompressedOops` | 压缩指针优化 | 减少内存使用 |

> 📌 **GraalVM JIT 模式**：使用 `ghcr.io/graalvm/jdk-community:25` 替换 `eclipse-temurin:25-jre-alpine`，内存额外降低 ~67MiB

---

### 2️⃣ MySQL 内存优化

**docker-compose.yml 配置**:

```yaml
command: >
  --innodb_buffer_pool_size=96M
  --innodb_log_buffer_size=4M
  --innodb_flush_log_at_trx_commit=2
  --sync_binlog=100
  --max_connections=30
  --table_open_cache=100
  --thread_cache_size=5
  --performance_schema=OFF
  --skip-name-resolve
  --tmp_table_size=4M
  --max_heap_table_size=4M
  --innodb_flush_method=O_DSYNC
```

**数据库 Schema 优化**（2026-04-27）：

| 表 | 列 | 原类型 | 新类型 | 原因 |
|:---|:---|:-------|:------|:-----|
| `t_tag` | `tag_name` | `varchar(20)` | `varchar(50)` | 长标签名报错 Error 1406 |
| `t_category` | `category_name` | `varchar(20)` | `varchar(50)` | 同上，预防性修复 |

```sql
ALTER TABLE t_tag MODIFY COLUMN tag_name varchar(50) NOT NULL;
ALTER TABLE t_category MODIFY COLUMN category_name varchar(50) NOT NULL;
```

---

### 3️⃣ Redis 内存优化

**docker-compose.yml 配置**:

```yaml
# SpringBoot 版
command: redis-server --requirepass aqi1015 --maxmemory 48mb --maxmemory-policy allkeys-lru

# Go 版
command: redis-server --requirepass aqi1015 --maxmemory 32mb --maxmemory-policy allkeys-lru --save ""
```

**优化说明**：

| 配置项 | 说明 |
|:-------|:-----|
| `--maxmemory 48mb/32mb` | 限制最大内存 |
| `--maxmemory-policy allkeys-lru` | LRU 淘汰策略 |
| `--save ""` | Go 版禁用 RDB 持久化 |

---

### 4️⃣ Elasticsearch 内存优化

**SpringBoot 版 docker-compose.yml**（ES 8.19.14）:

```yaml
environment:
  discovery.type: single-node
  ES_JAVA_OPTS: "-Xms128m -Xmx160m"
  TZ: Asia/Shanghai
  bootstrap.memory_lock: "true"
  xpack.security.enabled: "false"
  indices.query.bool.max_clause_count: "1024"
  indices.memory.index_buffer_size: "20%"
  indices.fielddata.cache.size: "10%"
```

**Go 版 docker-compose-go.yml**（ES 8.19.14，深度优化）:

```yaml
environment:
  discovery.type: single-node
  ES_JAVA_OPTS: "-Xms96m -Xmx160m"
  TZ: Asia/Shanghai
  xpack.security.enabled: "false"
  # 禁用 GeoIP 下载（博客不需要，节省 ~30MiB）
  ingest.geoip.downloader.enabled: "false"
  # 关闭 memory_lock（允许 swap 释放）
  bootstrap.memory_lock: "false"
  # Lucene 内存优化
  indices.memory.index_buffer_size: "5%"
  indices.fielddata.cache.size: "5%"
  indices.breaker.total.limit: "50%"
  # 关闭机器学习（省内存）
  xpack.ml.enabled: "false"
  # 关闭 watcher（告警功能，博客不需要）
  xpack.watcher.enabled: "false"
  # ILM 优化：减少历史索引
  indices.lifecycle.history_index_enabled: "false"
deploy:
  resources:
    limits:
      memory: 640M
    reservations:
      memory: 384M
```

> 📎 **IK 分词器安装地址**：https://release.infinilabs.com/analysis-ik/stable/elasticsearch-analysis-ik-8.19.14.zip

---

## 🐹 Go 后端部署

Go 后端编译为静态二进制，运行在极简 Alpine 镜像中：

```yaml
aurora-go:
  image: alpine:3.20
  container_name: aurora-go
  environment:
    TZ: Asia/Shanghai
    AURORA_MYSQL_HOST: aurora-mysql
    AURORA_REDIS_HOST: aurora-redis
    AURORA_RABBITMQ_HOST: aurora-rabbitmq
    AURORA_SEARCH_MODE: elasticsearch
    AURORA_UPLOAD_MODE: minio
    # ... 更多环境变量见 docker-compose-go.yml
  ports:
    - "8080:8080"
  volumes:
    - /opt/aurora/app/aurora-server:/app/aurora-server
    - /opt/aurora/app/configs:/app/configs
  command: ["/app/aurora-server", "--config", "configs/config.yaml"]
```

**交叉编译**（Windows → Linux）：

```powershell
$env:CGO_ENABLED="0"; $env:GOOS="linux"; $env:GOARCH="amd64"
cd aurora-go
go build -ldflags="-w -s" -o aurora-server-linux ./cmd/server
```

**Go 内存优势根因**：

| 因素 | Java | Go |
|:-----|:-----|:----|
| 运行时 | JVM（JRE ~180MB） | 静态二进制（~15MB） |
| GC 开销 | 需要预留堆空间 | 仅需 ~10MB GC 元数据 |
| 线程模型 | 1:1 OS 线程，每线程 ~1MB 栈 | M:N 协程，初始栈 2KB |
| 内存管理 | 堆+元空间+CodeCache+直接内存 | Go runtime 统一管理 |
| JIT 缓存 | C2/Graal 编译缓存 ~32-48MB | AOT 编译，无 JIT 缓存 |

---

## 📦 部署文件更新

| 文件 | 路径 | 说明 |
|:-----|:-----|:-----|
| 📄 docker-compose.yml | `/opt/aurora/docker-compose.yml` | SpringBoot 后端 + 中间件 |
| 📄 docker-compose-go.yml | `/opt/aurora/docker-compose-go.yml` | Go 后端 + 中间件（深度优化） |
| 🐳 Dockerfile | `/opt/aurora/app/Dockerfile` | SpringBoot JVM 优化 |
| 🐳 Dockerfile (Go) | `aurora-go/Dockerfile` | Go 多阶段构建 (~15MB) |
| 🐳 Dockerfile.graalvm | `aurora-springboot/Dockerfile.graalvm` | GraalVM JIT 模式 |

---

## 🔄 重启服务说明

### SpringBoot 模式

```bash
cd /opt/aurora
docker compose down
docker compose up -d
```

### Go 模式

```bash
cd /opt/aurora
docker compose -f docker-compose-go.yml down
docker compose -f docker-compose-go.yml up -d
```

### 切换后端

```bash
# 停 Java 后端，启 Go 后端
docker stop aurora-springboot && docker rm aurora-springboot
docker compose -f docker-compose-go.yml up -d aurora-go

# 停 Go 后端，启 Java 后端
docker stop aurora-go && docker rm aurora-go
docker compose up -d springboot
```

---

## ✅ 验证优化效果

```bash
# 查看所有容器内存使用
docker stats --no-stream --format 'table {{.Name}}\t{{.MemUsage}}\t{{.MemPerc}}'

# 查看服务器内存使用
free -h

# 验证 MySQL 配置
docker exec aurora-mysql mysql -uroot -paqi1015 -e "SHOW VARIABLES LIKE 'innodb_buffer_pool_size';"

# Go 后端健康检查
curl http://localhost:8080/health

# Java 后端健康检查
curl http://localhost:8080/
```

---

## ⚠️ 注意事项

| 序号 | 注意事项 |
|:----:|:---------|
| 1 | **MySQL 连接数限制**：`max_connections=30`，高并发场景可能需要调整 |
| 2 | **Redis 持久化**：Go 版已禁用 RDB，如需持久化请配置 AOF |
| 3 | **Elasticsearch 性能**：128-160MB 堆内存适合小规模索引，数据量大时需增加 |
| 4 | **SpringBoot 4.x 元空间**：需要 `128m-192m` 元空间（比 3.x 需求更大） |
| 5 | **Go 后端 Agent**：AI Agent 模块默认关闭（`agent.enabled: false`），开启不影响核心功能 |
| 6 | **数据库 Schema**：`tag_name`/`category_name` 已扩展至 varchar(50)，Go 验证规则已同步更新 |

---

## 📝 优化总结

### 1️⃣ 第一轮优化 (2026-03-06)

通过精细调整 JVM 参数、数据库配置和中间件设置：

- ✅ 总内存使用降低约 **453MiB**
- ✅ 可用内存从 933MiB 增加到 **1.3GiB**
- ✅ 系统内存占用率从 **72.3% 降至 60.6%**
- ✅ SpringBoot 堆内存从 256MB-384MB 降至 192MB-384MB

---

### 2️⃣ 第二轮极限优化 (2026-03-07)

进一步压缩内存配置：

- ✅ SpringBoot 堆内存从 192MB-384MB 降至 **96MB-224MB**（极限值）
- ✅ Elasticsearch 堆内存从 64MB-128MB 升级到 **96MB-192MB**（IK分词器需求）
- ✅ 元空间、代码缓存全面压缩
- ✅ 总内存进一步降低约 **20MiB**

---

### 3️⃣ 第三轮稳定性优化 (2026-03-09)

针对 Elasticsearch OOM 问题进行调整：

- ✅ SpringBoot 堆内存从 96MB-224MB 降至 **64MB-128MB**（超极限值）
- ✅ Elasticsearch 堆内存调整到 **64MB-160m**（中等值平衡性能和内存）
- ✅ MySQL 连接数从 50 降至 **30**，缓冲池从 128M 降至 **96M**
- ✅ Redis 最大内存从 32MB 降至 **24MB**
- ✅ 安装 IK 中文分词器插件
- ✅ 断路器优化：total.limit 从 60% 提升至 **85%**
- ✅ article 索引重建成功：24 篇文章全部同步
- ✅ 总内存降低约 **67MiB**

---

### 🎯 第三轮最终效果

| 指标 | 数值 |
|:-----|:-----|
| 📊 当前总内存占用 | **1539.44MiB** |
| 💾 总共节省 | **~450MiB**（从 1.9GiB 降至 1.51GiB） |
| 📈 内存占用率 | **72.7%**（初始 72.3%） |
| 🔍 Elasticsearch | 稳定性显著提升，解决 OOM 问题 |
| 🔤 IK 分词器 | 已安装并正常运行 |

---

### 🔑 关键优化策略

| 序号 | 策略 |
|:----:|:-----|
| 1 | **JVM超极限压缩**：48MB-96MB堆内存 + 128MB-192MB元空间 + 24MB代码缓存 |
| 2 | **数据库深度优化**：连接池30、缓冲池96M、表缓存100等极限调优 |
| 3 | **中间件轻量化**：Redis 禁用持久化、48MB内存限制 |
| 4 | **垃圾回收优化**：SpringBoot使用SerialGC，Elasticsearch使用默认GC |
| 5 | **ES稳定性增强**：配置断路器、合并线程、索引缓存等防止OOM |
| 6 | **IK分词器支持**：已安装 `elasticsearch-analysis-ik-8.19.14` 支持中文分词 |
| 7 | **双后端架构**：Go 后端无 JVM 开销，内存仅需 ~40MiB |

---

## 🔍 验证命令

### 查看当前内存使用

```bash
# 查看所有容器内存使用
docker stats --no-stream --format 'table {{.Name}}\t{{.MemUsage}}\t{{.MemPerc}}'

# 计算总内存占用
docker stats --no-stream --format '{{.MemUsage}}' | awk -F'/' '{print $1}' | awk '{sum+=$1} END {print sum"MiB"}'

# 查看服务器内存使用
free -h
```

### 验证各组件配置

#### ✅ 验证 SpringBoot JVM 参数

```bash
# 查看JVM运行参数
docker exec aurora-springboot jcmd 1 VM.flags

# 查看堆内存使用
docker exec aurora-springboot jmap -heap 1

# 检查 JDK 版本
docker exec aurora-springboot java -version
```

#### ✅ 验证 Go 后端

```bash
# 健康检查
curl http://localhost:8080/health

# 查看内存
docker stats --no-stream | grep aurora-go

# 查看配置
docker exec aurora-go cat /app/configs/config.yaml
```

#### ✅ 验证 MySQL 配置

```bash
# 查看缓冲池大小
docker exec aurora-mysql mysql -uroot -paqi1015 -e "SHOW VARIABLES LIKE 'innodb_buffer_pool_size';"

# 查看最大连接数
docker exec aurora-mysql mysql -uroot -paqi1015 -e "SHOW VARIABLES LIKE 'max_connections';"

# 查看 tag_name 列宽（2026-04-27 扩展）
docker exec aurora-mysql mysql -uroot -paqi1015 -e "SHOW COLUMNS FROM t_tag WHERE Field = 'tag_name';"
docker exec aurora-mysql mysql -uroot -paqi1015 -e "SHOW COLUMNS FROM t_category WHERE Field = 'category_name';"
```

#### ✅ 验证 Redis 配置

```bash
# 查看最大内存配置
docker exec aurora-redis redis-cli -a aqi1015 CONFIG GET maxmemory

# 查看淘汰策略
docker exec aurora-redis redis-cli -a aqi1015 CONFIG GET maxmemory-policy
```

#### ✅ 验证 Elasticsearch 配置

```bash
# 查看JVM参数
docker exec aurora-elasticsearch env | grep ES_JAVA_OPTS

# 查看已安装插件
docker exec aurora-elasticsearch elasticsearch-plugin list

# 测试 IK 分词器
curl -X POST "localhost:9200/_analyze" -H 'Content-Type: application/json' -d '{
  "analyzer": "ik_max_word",
  "text": "中华人民共和国国歌"
}'
```

---

## 🚀 第四轮优化：GraalVM JIT 模式迁移 (2026-03-27)

### 迁移目标

将 SpringBoot 后端服务从 OpenJDK/TencentKona JDK 25 迁移到 **GraalVM JDK 25** (JIT 模式)

### 迁移优势

| 优势 | 说明 |
|:-----|:-----|
| ✅ 零代码修改 | 完全兼容现有代码，迁移成本为 0 |
| ✅ 性能提升 | Graal JIT 编译器优化，预期 10-20% 性能提升 |
| ✅ 内存优化 | 更高效的内存管理，实测 **-19.5%** 内存节省 |
| ✅ 启动加速 | 优化的类加载机制，启动时间持平或略快 |
| ✅ 向后兼容 | 完全兼容 HotSpot JVM，无兼容性风险 |

### 实际收益（实测数据）

| 指标 | HotSpot JVM | GraalVM JIT | 改善幅度 |
|:------|:-----------:|:------------:|:--------:|
| **内存占用** | 342.8 MiB | 275.9 MiB | ⬇️ **-19.5%** |
| **堆内存配置** | -Xms64m -Xmx128m | -Xms64m -Xmx128m | 一致 |
| **状态** | 正常运行 | 正常运行 | - |

### 迁移文件

| 文件 | 说明 |
|:-----|:-----|
| `Dockerfile.graalvm` | GraalVM Dockerfile |
| `docker-compose-graalvm.yml` | GraalVM Compose 配置 |
| `deploy-graalvm.sh` | 部署说明文档 |

### 快速迁移命令

```bash
# 1. 停止现有服务
docker stop aurora-springboot && docker rm aurora-springboot

# 2. 启动 GraalVM 服务
docker run -d --name aurora-springboot --network aurora-network -p 8080:8080 \
  -v /opt/aurora/app/aurora-springboot-0.0.1.jar:/app/blog.jar \
  -e SPRING_PROFILES_ACTIVE=prod -e TZ=Asia/Shanghai \
  -e SPRING_DATASOURCE_URL="jdbc:mysql://aurora-mysql:3306/aurora?serverTimezone=Asia/Shanghai&allowMultiQueries=true&useSSL=false&allowPublicKeyRetrieval=true" \
  -e SPRING_DATA_REDIS_HOST=aurora-redis -e SPRING_RABBITMQ_HOST=aurora-rabbitmq \
  -e ELASTICSEARCH_REST_URIS=http://aurora-elasticsearch:9200 \
  -e UPLOAD_MINIO_ENDPOINT=http://aurora-minio:9000/ \
  --restart=always ghcr.io/graalvm/jdk-community:25 \
  java -Xms64m -Xmx128m -XX:+UseSerialGC -XX:MetaspaceSize=64m -XX:MaxMetaspaceSize=128m \
       -XX:ReservedCodeCacheSize=32m -Xss256k -XX:+UseCompressedOops -XX:+UseCompressedClassPointers \
       -XX:+UseStringDeduplication -XX:+OptimizeStringConcat \
       -Djava.security.egd=file:/dev/./urandom -Dspring.jmx.enabled=false -jar /app/blog.jar

# 3. 查看日志
docker logs -f aurora-springboot
```

### GraalVM 自动启用的优化

| 优化 | 说明 |
|:-----|:-----|
| ✅ **Graal JIT 编译器** | 替代 C2 编译器，性能提升 10-20% |
| ✅ **更激进的内联** | 方法调用优化，减少 5-10% 开销 |
| ✅ **更好的逃逸分析** | 栈上分配优化，减少堆压力 |
| ✅ **优化的死代码消除** | 减少无效代码，提升 CPU 效率 |

### 注意事项

| 序号 | 注意事项 |
|:----:|:---------|
| 1 | **预热期**：GraalVM 需要运行 10-30 分钟才能体现优化效果 |
| 2 | **镜像说明**：GraalVM 官方没有 Alpine 版本，只有 Oracle Linux |
| 3 | **回滚方案**：保留原配置，可快速回滚到 HotSpot JVM |
| 4 | **生产部署**：建议先在测试环境验证，再灰度发布到生产 |

---

## 🚀 第五轮优化：Spring Boot 4.x + Go 双后端 (2026-04-27)

### 升级概要

| 项目 | 旧版本 | 新版本 |
|:-----|:-------|:-------|
| Spring Boot | 3.2.x | **4.1.0-M4** |
| JDK | 21 | **25** |
| Elasticsearch Client | 7.17.x | **8.19.14** (原生 Java Client) |
| MySQL Connector | 8.0.33 | **9.6.0** |
| Go 后端 | 无 | **Go 1.26 + Gin 1.10** |

### Spring Boot 4.x JVM 参数调整

Spring Boot 4.x 对元空间需求增大，JVM 参数已相应调整：

| 参数 | 旧值（3.x） | 新值（4.x） | 说明 |
|:-----|:-----------:|:-----------:|:-----|
| `-Xms` | 64m | **48m** | 进一步压缩 |
| `-Xmx` | 128m | **96m** | 进一步压缩 |
| `MetaspaceSize` | 64m | **128m** | 4.x 需要更多元空间 |
| `MaxMetaspaceSize` | 128m | **192m** | 4.x 需要更多元空间 |
| `ReservedCodeCacheSize` | 32m | **24m** | 进一步压缩 |

### Go 后端技术栈

```
Web框架:   Gin v1.10
ORM:       GORM v1.30
缓存:      go-redis/v9
消息队列:   amqp091-go
搜索:       go-elasticsearch/v8 (8.19)
对象存储:   minio-go/v7
认证:       golang-jwt/v5 + RBAC
配置:       Viper + YAML
日志:       Zap (结构化)
定时任务:   robfig/cron/v3
Agent引擎:  tRPC-Agent-Go v1.8 (可选)
```

### Go 后端内部架构

```
aurora-go/internal/
├── agent/        🤖 AI Agent (12 files, tRPC-Agent-Go)
├── config/       ⚙️ 配置管理
├── constant/     📋 常量枚举
├── consumer/     📨 MQ 消费者
├── dto/          📦 数据传输对象
├── errors/       ❌ 错误码定义
├── handler/      🌐 HTTP 处理器 (23 files)
├── infrastructure/ 🏗️ 基础设施
├── middleware/    🔐 中间件 JWT/RBAC/限流/CORS (9 files)
├── model/        📊 GORM 数据模型 (16 files)
├── scheduler/    ⏰ 定时任务
├── service/      💼 业务逻辑层 (27 files)
├── strategy/     🎯 策略模式 (9 files, 搜索/上传)
├── util/         🔧 工具函数 (10 files)
└── vo/           📤 视图对象
```

### 数据库 Schema 修复

修复了长标签名/分类名导致的 Error 1406：

```sql
-- t_tag.tag_name: varchar(20) → varchar(50)
ALTER TABLE t_tag MODIFY COLUMN tag_name varchar(50) NOT NULL;

-- t_category.category_name: varchar(20) → varchar(50)
ALTER TABLE t_category MODIFY COLUMN category_name varchar(50) NOT NULL;
```

Go 验证规则同步更新：
- `vo/extra_vo.go`: `max=20` → `max=50`
- `dto/handler_vo.go`: `max=20` → `max=50`

### ES 深度优化（Go 版 docker-compose-go.yml）

Go 版的 ES 配置增加了更多优化项：

| 配置项 | 说明 | 预计节省 |
|:-------|:-----|:---------|
| `ingest.geoip.downloader.enabled: false` | 禁用 GeoIP 下载 | ~30 MiB |
| `xpack.ml.enabled: false` | 关闭机器学习 | ~50 MiB |
| `xpack.watcher.enabled: false` | 关闭告警 | ~20 MiB |
| `indices.lifecycle.history_index_enabled: false` | 减少 ILM 历史索引 | ~10 MiB |
| `bootstrap.memory_lock: false` | 允许 swap 释放 | 灵活 |
| `memory limit: 640M` | 容器级内存硬限制 | 防止 OOM |

---

<p align="center">

| 项目 | 数值 |
|:-----|:-----|
| 📅 **文档最后更新时间** | 2026-04-27 |
| 💾 **总内存占用 (Java)** | **~1472 MiB** (GraalVM JIT) |
| 💾 **总内存占用 (Go)** | **~1180 MiB** |
| ⚡ **Go 后端内存** | **~40 MiB** (vs Java ~280 MiB) |
| 📊 **内存占用率 (Go)** | **~56%** |
| 🔤 **IK 分词器** | elasticsearch-analysis-ik-8.19.14 |
| ☕ **Java 版本** | GraalVM JDK 25 (JIT 模式) |
| 🐹 **Go 版本** | Go 1.26 + Gin 1.10 |
| 🏗️ **Spring Boot** | 4.1.0-M4 |
| 🔍 **ES 版本** | 8.19.14 (Java) / 8.19.14 (Go) |

</p>

---
